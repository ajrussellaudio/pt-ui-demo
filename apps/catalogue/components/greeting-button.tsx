"use client";
// Next >13 I hate you so much

import { Button } from "@repo/ui";

export const GreetingButton = () => {
  return <Button onClick={() => alert("Quiet in the library!")}>Click here!</Button>;
};
